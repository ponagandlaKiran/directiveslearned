import { LearningDirective } from './learning.directive';

describe('LearningDirective', () => {
  it('should create an instance', () => {
    const directive = new LearningDirective();
    expect(directive).toBeTruthy();
  });
});
